// CONVERTING MODULE HEADER
#ifndef _CONVERTING_H_
#define _CONVERTING_H_

// include standart input/output library
#include <stdio.h>

// include standart library so we can use size_t and atoi
#include <stdlib.h>

// include "string" linbrary so we can use string fucntions
#include <string.h>

// Converts string to int
void converting(void);
#endif
